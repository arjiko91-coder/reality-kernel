[project]
name = "reality-kernel"
version = "0.1.0"
description = "Agentic physics simulation laboratory with benchmark-driven self-correction."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
